name: Release

# A v* tag builds every platform, publishes the GitHub release, then hands
# over to packaging.yml for .deb, .rpm, AUR, Homebrew and WinGet recipes.
# Running it by hand builds everything the same way without publishing, to
# try a change to this workflow before tagging.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  release-source:
    name: tag is on main
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Require the release commit on main
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          git fetch --no-tags origin refs/heads/main:refs/remotes/origin/main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" refs/remotes/origin/main; then
            echo "Release tags must point to a commit already on origin/main." >&2
            exit 1
          fi
      - name: Version
        id: version
        run: |
          version=$(sed -n 's/^version = "\(.*\)"$/\1/p' Cargo.toml | head -n 1)
          if [[ "$GITHUB_REF" == refs/tags/* ]] && [ "$GITHUB_REF_NAME" != "v$version" ]; then
            echo "::error::Tag $GITHUB_REF_NAME doesn't match Cargo.toml version $version."
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

  build:
    name: build (${{ matrix.target }})
    needs: release-source
    permissions:
      contents: read
      id-token: write
      attestations: write
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          # Static musl binaries run on any Linux distribution.
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-musl
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-musl
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            msi-arch: x64
          # Cross-compiled from x64; ring builds its assembly with the
          # runner's clang.
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            msi-arch: arm64
    env:
      VERSION: ${{ needs.release-source.outputs.version }}
      # Azure Artifact Signing (formerly Trusted Signing). A complete set
      # signs the Windows binaries and installer; none leaves them unsigned.
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      # A code signing certificate as an alternative to Azure.
      WINDOWS_CERTIFICATE_PFX: ${{ secrets.WINDOWS_CERTIFICATE_PFX }}
      WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: musl toolchain
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools
      # No compiler cache: published binaries never build from outputs
      # restored from an Actions cache.
      - run: cargo build --locked --release --target ${{ matrix.target }} --bins

      - name: Check the binary is static
        if: runner.os == 'Linux'
        run: |
          bin="target/${{ matrix.target }}/release/cww"
          if readelf -d "$bin" | grep -q NEEDED; then
            readelf -d "$bin"
            echo "::error::$bin links shared libraries"
            exit 1
          fi
          "$bin" --version

      - name: Sign binaries (Azure Artifact Signing)
        if: runner.os == 'Windows' && env.AZURE_CLIENT_ID != ''
        uses: azure/artifact-signing-action@v2
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_SIGNING_ENDPOINT }}
          signing-account-name: ${{ secrets.AZURE_SIGNING_ACCOUNT }}
          certificate-profile-name: ${{ secrets.AZURE_SIGNING_PROFILE }}
          files: |
            ${{ github.workspace }}\target\${{ matrix.target }}\release\cww.exe
            ${{ github.workspace }}\target\${{ matrix.target }}\release\cww-agent.exe
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256
          description: Chat with Work Local Agent
      - name: Sign binaries (certificate)
        if: runner.os == 'Windows' && env.AZURE_CLIENT_ID == '' && env.WINDOWS_CERTIFICATE_PFX != ''
        shell: pwsh
        run: |
          ./packaging/windows/sign.ps1 `
            "target\${{ matrix.target }}\release\cww.exe" `
            "target\${{ matrix.target }}\release\cww-agent.exe"

      - name: Package
        shell: bash
        run: |
          name="cww-v${VERSION}-${{ matrix.target }}"
          mkdir -p "dist/$name"
          cp README.md PROTOCOL.md CONTROL.md SECURITY.md LICENSE-MIT LICENSE-APACHE "dist/$name/"
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp "target/${{ matrix.target }}/release/cww.exe" "target/${{ matrix.target }}/release/cww-agent.exe" "dist/$name/"
            (cd dist && 7z a "$name.zip" "$name")
          else
            cp "target/${{ matrix.target }}/release/cww" "dist/$name/"
            cp --parents packaging/systemd/cww.service "dist/$name/"
            tar -C dist -czf "dist/$name.tar.gz" "$name"
          fi
          rm -rf "dist/$name"

      # WiX 5 is a .NET tool; the runner has the SDK.
      - name: Installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          dotnet tool install --global wix --version 5.0.2
          ./packaging/windows/build-msi.ps1 -Version $env:VERSION -Arch ${{ matrix.msi-arch }} `
            -Binary "target\${{ matrix.target }}\release\cww.exe" `
            -Output "dist\cww-v$($env:VERSION)-${{ matrix.target }}.msi"
          Remove-Item dist\*.wixpdb
      - name: Sign installer (Azure Artifact Signing)
        if: runner.os == 'Windows' && env.AZURE_CLIENT_ID != ''
        uses: azure/artifact-signing-action@v2
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_SIGNING_ENDPOINT }}
          signing-account-name: ${{ secrets.AZURE_SIGNING_ACCOUNT }}
          certificate-profile-name: ${{ secrets.AZURE_SIGNING_PROFILE }}
          files-folder: ${{ github.workspace }}\dist
          files-folder-filter: msi
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256
          description: Chat with Work Local Agent
      - name: Sign installer (certificate)
        if: runner.os == 'Windows' && env.AZURE_CLIENT_ID == '' && env.WINDOWS_CERTIFICATE_PFX != ''
        shell: pwsh
        run: ./packaging/windows/sign.ps1 (Get-ChildItem dist\*.msi).FullName

      - name: Attest release packages
        if: startsWith(github.ref, 'refs/tags/')
        uses: crmne/native-packages/.github/actions/attest@dc0cb1586894ca0239c53fd02d4da8064712671c # v0.7.0
        with:
          subject-path: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.msi
      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.target }}
          path: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.msi
          if-no-files-found: error

  macos:
    name: macOS universal
    needs: release-source
    permissions:
      contents: read
      id-token: write
      attestations: write
    runs-on: macos-latest
    timeout-minutes: 60
    env:
      VERSION: ${{ needs.release-source.outputs.version }}
      # Complete Apple credentials make native-packages sign the binary with
      # Developer ID and the hardened runtime, and notarize it.
      APPLE_CERTIFICATE_P12: ${{ secrets.APPLE_CERTIFICATE_P12 }}
      APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
      # A Developer ID Installer identity signs the .pkg, which is then
      # notarized and stapled with the credentials above.
      APPLE_INSTALLER_CERTIFICATE_P12: ${{ secrets.APPLE_INSTALLER_CERTIFICATE_P12 }}
      APPLE_INSTALLER_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_INSTALLER_CERTIFICATE_PASSWORD }}
      APPLE_INSTALLER_SIGNING_IDENTITY: ${{ secrets.APPLE_INSTALLER_SIGNING_IDENTITY }}
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin
      # No compiler cache, as above.
      - name: Build both architectures
        run: |
          cargo build --locked --release --target aarch64-apple-darwin --bin cww
          cargo build --locked --release --target x86_64-apple-darwin --bin cww

      - name: Universal binary
        run: |
          mkdir -p universal
          lipo -create \
            target/aarch64-apple-darwin/release/cww \
            target/x86_64-apple-darwin/release/cww \
            -output universal/cww
          lipo universal/cww -verify_arch x86_64 arm64

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '4.0'
      - name: Install native packaging
        run: gem install native-packages --version 0.7.0 --no-document

      - name: Sign and notarize the universal binary
        run: |
          name="cww-v${VERSION}-macos-universal"
          mkdir -p "$RUNNER_TEMP/portable-input" dist
          cp universal/cww README.md PROTOCOL.md CONTROL.md SECURITY.md LICENSE-MIT LICENSE-APACHE "$RUNNER_TEMP/portable-input/"
          native-packages notarize-macos "$RUNNER_TEMP/portable-input" \
            --output "$RUNNER_TEMP/signed-portable/$name"
          tar -C "$RUNNER_TEMP/signed-portable" -czf "dist/$name.tar.gz" "$name"
          cp "$RUNNER_TEMP/signed-portable/$name/cww" "$RUNNER_TEMP/cww-signed"

      - name: Verify the signature
        if: env.APPLE_SIGNING_IDENTITY != ''
        run: |
          codesign --verify --strict --verbose=2 "$RUNNER_TEMP/cww-signed"
          codesign --display --verbose=2 "$RUNNER_TEMP/cww-signed" 2>&1 | grep -q "flags=0x10000(runtime)"

      - name: Installer package
        run: |
          if [ -n "$APPLE_INSTALLER_CERTIFICATE_P12" ]; then
            APPLE_INSTALLER_KEYCHAIN=$(bash packaging/macos/import-installer-identity.sh)
            export APPLE_INSTALLER_KEYCHAIN
          fi
          bash packaging/macos/pkg.sh "$RUNNER_TEMP/cww-signed" "$VERSION" \
            "dist/cww-v${VERSION}-macos-universal.pkg"

      - name: Attest macOS packages
        if: startsWith(github.ref, 'refs/tags/')
        uses: crmne/native-packages/.github/actions/attest@dc0cb1586894ca0239c53fd02d4da8064712671c # v0.7.0
        with:
          subject-path: |
            dist/*.tar.gz
            dist/*.pkg
      - uses: actions/upload-artifact@v7
        with:
          name: macos-universal
          path: |
            dist/*.tar.gz
            dist/*.pkg
          if-no-files-found: error

  publish:
    name: GitHub release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [release-source, build, macos]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true
      - name: Installer scripts
        run: |
          cp packaging/install.sh dist/cww-installer.sh
          cp packaging/install.ps1 dist/cww-installer.ps1
      - name: Checksums
        run: |
          cd dist
          sha256sum -- * > checksums.txt
          cat checksums.txt
      # Publish the reviewed notes committed before the tag, never a
      # generated summary. A stable tag without them stops here; a
      # prerelease without them gets a short placeholder.
      - name: Release notes
        run: |
          notes="packaging/release-notes/${GITHUB_REF_NAME}.md"
          if [ -f "$notes" ]; then
            cp "$notes" release-notes.md
          elif [[ "$GITHUB_REF_NAME" == *-* ]]; then
            echo "Prerelease build of the Chat with Work Local Agent ${GITHUB_REF_NAME}." > release-notes.md
          else
            echo "::error::Write $notes and push it before tagging a stable release."
            exit 1
          fi
      - uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          name: Chat with Work Local Agent ${{ needs.release-source.outputs.version }}
          body_path: release-notes.md
          generate_release_notes: false
          draft: ${{ contains(github.ref_name, '-') }}
          prerelease: ${{ contains(github.ref_name, '-') }}
          make_latest: ${{ !contains(github.ref_name, '-') }}

  packaging:
    permissions:
      contents: write
    if: startsWith(github.ref, 'refs/tags/v') && !contains(github.ref_name, '-')
    needs: publish
    uses: ./.github/workflows/packaging.yml
    with:
      version: ${{ github.ref_name }}
    secrets: inherit
